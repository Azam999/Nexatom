import React from 'react';
import { Container } from 'react-bootstrap';

const ML: React.FC = ({}) => {
    return (
        <Container>
            ML
        </Container>
    );
};

export default ML;